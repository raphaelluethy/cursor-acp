{
  "name": "@raphael/cursor-acp",
  "version": "0.9.0",
  "description": "ACP adapter backed by the Cursor SDK",
  "main": "dist/lib.js",
  "types": "dist/lib.d.ts",
  "bin": {
    "cursor-acp": "./dist/index.js"
  },
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/lib.d.ts",
      "import": "./dist/lib.js"
    },
    "./*": "./*"
  },
  "files": [
    "dist/*.js",
    "dist/*.d.ts",
    "dist/*.d.ts.map",
    "scripts/patch-cursor-sdk-attribution.mjs",
    "README.md",
    "LICENSE",
    "NOTICE",
    "package.json"
  ],
  "scripts": {
    "postinstall": "node scripts/patch-cursor-sdk-attribution.mjs",
    "build": "tsc",
    "build:clean": "rm -rf dist && tsc",
    "start": "node dist/index.js",
    "dev": "nub run build && node dist/index.js",
    "lint": "oxlint src",
    "lint:fix": "oxlint src --fix",
    "lint:eslint": "eslint src --ext .ts",
    "lint:eslint:fix": "eslint src --ext .ts --fix",
    "format": "oxfmt --config .oxfmtrc.json --write src",
    "format:check": "oxfmt --config .oxfmtrc.json --check src",
    "format:prettier": "prettier --write .",
    "format:prettier:check": "prettier --check .",
    "typecheck": "tsgo --noEmit",
    "check": "nub run lint && nub run format:check",
    "check:legacy": "nub run lint:eslint && nub run format:prettier:check",
    "test": "vitest",
    "test:run": "vitest run",
    "test:coverage": "vitest run --coverage",
    "prepublishOnly": "nub run build:clean"
  },
  "keywords": [
    "cursor",
    "acp",
    "agent",
    "typescript",
    "adapter"
  ],
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.13"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "1.4.0",
    "@cursor/sdk": "1.0.28",
    "@typescript/native-preview": "^7.0.0-dev.20260430.1",
    "oxfmt": "^0.47.0",
    "oxlint": "^1.62.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@typescript-eslint/eslint-plugin": "^8.59.1",
    "@typescript-eslint/parser": "^8.59.1",
    "eslint": "^10.2.1",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.5.0",
    "oxlint-tsgolint": "^0.22.1",
    "prettier": "^3.8.3",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "devEngines": {
    "packageManager": {
      "name": "nub",
      "version": "^0.7.5",
      "onFail": "ignore"
    }
  }
}
